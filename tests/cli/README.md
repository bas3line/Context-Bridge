# CLI fixtures

CLI acceptance coverage lives in `crates/cb-cli/tests/vertical_slice.rs` so
Cargo discovers it with the `cb` binary. This directory is reserved for
additional black-box command fixtures.
