# Integration fixtures

The vertical-slice integration test creates its repository, executables,
database, and runtime captures in a temporary directory. No credentials or
installed AI coding agents are used.
