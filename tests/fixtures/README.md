# Fixture policy

All fixtures are synthetic and contain no real credentials, conversation
exports, hostnames, or private paths. Values that resemble secrets exist only
to verify that target handoffs exclude them.
